# Grafana build configuration

Reused from the local official create-plugin 7.11.0 scaffold. See ../THIRD_PARTY.md.
Keep project overrides in root configuration files rather than editing generated files here.
Webpack emits the plugin into dist/. Docker and Playwright are configured at the repository root.
